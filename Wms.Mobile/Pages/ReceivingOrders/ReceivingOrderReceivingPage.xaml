<ContentPage
    x:Class="Wms.Mobile.ReceivingOrderReceivingPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:contracts="clr-namespace:Wms.Contracts.Mobile.V1;assembly=Wms.Contracts"
    Title="Приёмка">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="16">
            <Label
                FontAttributes="Bold"
                FontSize="26"
                Text="{Binding Order.Number, StringFormat='Приёмка ордера {0}'}" />
            <Label x:Name="StatusLabel" FontAttributes="Bold" FontSize="18" />

            <Border Padding="16">
                <VerticalStackLayout Spacing="6">
                    <Label Text="{Binding Order.WarehouseName, StringFormat='Склад: {0}'}" />
                    <Label Text="{Binding Order.ShipperName, StringFormat='Отправитель: {0}'}" />
                    <Label x:Name="LocationLabel" />
                    <Label x:Name="ProgressLabel" FontAttributes="Bold" />
                </VerticalStackLayout>
            </Border>

            <Label
                Text="Плановые строки"
                FontAttributes="Bold"
                FontSize="20" />
            <VerticalStackLayout
                BindableLayout.ItemsSource="{Binding Lines}"
                Spacing="10">
                <BindableLayout.ItemTemplate>
                    <DataTemplate x:DataType="contracts:MobileReceivingOrderLineResponse">
                        <Border Padding="14">
                            <VerticalStackLayout Spacing="4">
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="17"
                                    Text="{Binding SkuName}" />
                                <Label Text="{Binding SkuCode, StringFormat='Код: {0}'}" />
                                <Label Text="{Binding LineNumber, StringFormat='Строка: {0}'}" />
                                <Label Text="{Binding PlanQuantity, StringFormat='План: {0:g}'}" />
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
